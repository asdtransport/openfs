# Getting Started with OpenFS

## Installation

```bash
bun add @openfs/core @openfs/adapter-sqlite just-bash
```

## Quick Start

1. Create a SQLite database with your content
2. Initialize the adapter
3. Pass to just-bash as the `fs` option
4. Run shell commands against your virtual filesystem

See the `examples/local-docs` directory for a complete working example.
